<div id="gonow"></div>
<p>choice</p>

<%# city_name = GOOGLE %>
<%# city_id = City.where(name:"#{city_name}").id %>
<% user_id = current_user.id %>
<%# choix = Trip.where(city_id:"#{city_id}", user_id:"#{user_id}") %>

<%#= simple_form_for @trip do |f| %>
  <%#= f.association :trip, collection: choix, priority: @bill.location %>
  <%#= f.button :submit %>
<%# end %>

# call api geoloc
# call a l'api geocoder
# le nom de ville
# City?find_by_name(name)
