<div class="wrapper-header">
  <div class="header">
    <h1 class="page-title">Your activities</h1>
    <%= render 'shared/antoine' if current_user%>
  </div>
</div>


<%= render "shared/trip_show", trip: @trip, trip_activities: @trip_activities, markers: @markers %>
