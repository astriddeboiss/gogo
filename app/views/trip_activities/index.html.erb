<h1> All my activities for <%=@trip.name %></h1>

<ul><% @trip_activities.each do |trip_activity| %>
<li> <%= trip_activity.activity.name %></li>
<% end %>
</ul>
<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>
