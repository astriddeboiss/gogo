<%= simple_form_for [@trip, @trip_activity] do |f| %>
  <%= f.association :activity, as: :check_boxes %>
  <%= f.submit "See my itinerary", class: "btn btn-primary" %>
  <%= f.input :remember_me, inline_label: 'Yes, remember me' %>
<%  end %>
